{
  "name": "ai-content-blocker",
  "version": "1.0.0",
  "description": "blocks content about ai or anything by a customizable word list,  design and developed at kiddokraft.",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run build.ts --watch"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "typescript": "^5.3.3"
  }
}
